version: '3.8'

services:
  identity-service:
    build:
      context: ./services/identity-service
      dockerfile: Dockerfile
    container_name: identity-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4000
    volumes:
      - ./services/identity-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  engineering-service:
    build:
      context: ./services/engineering-service
      dockerfile: Dockerfile
    container_name: engineering-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4001:4001"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4001
    volumes:
      - ./services/engineering-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  crm-service:
    build:
      context: ./services/crm-service
      dockerfile: Dockerfile
    container_name: crm-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4002:4002"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4002
    volumes:
      - ./services/crm-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  finance-service:
    build:
      context: ./services/finance-service
      dockerfile: Dockerfile
    container_name: finance-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4003:4003"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4003
    volumes:
      - ./services/finance-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  hr-service:
    build:
      context: ./services/hr-service
      dockerfile: Dockerfile
    container_name: hr-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4004:4004"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4004
    volumes:
      - ./services/hr-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  inventory-service:
    build:
      context: ./services/inventory-service
      dockerfile: Dockerfile
    container_name: inventory-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4005:4005"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4005
    volumes:
      - ./services/inventory-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  procurement-service:
    build:
      context: ./services/procurement-service
      dockerfile: Dockerfile
    container_name: procurement-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4006:4006"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4006
    volumes:
      - ./services/procurement-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  project-service:
    build:
      context: ./services/project-service
      dockerfile: Dockerfile
    container_name: project-service
    working_dir: /usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "4007:4007"
    environment:
      NODE_ENV: development
      DATABASE_URL: postgresql://postgres:postgres@host.docker.internal:5432/minierp
      PORT: 4007
    volumes:
      - ./services/project-service:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - mini-net

  gateway:
    image: nginx:stable
    container_name: gateway
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - identity-service
      - engineering-service
      - crm-service
      - finance-service
      - hr-service
      - inventory-service
      - procurement-service
      - project-service
    networks:
      - mini-net

networks:
  mini-net:
    driver: bridge
